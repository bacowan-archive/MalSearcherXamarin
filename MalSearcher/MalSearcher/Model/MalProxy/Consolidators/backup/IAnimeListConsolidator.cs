using Database.Context;
using Database.Objects;
using Microsoft.EntityFrameworkCore;
using System;
using System.Collections.Generic;
using System.Text;

namespace MalSearcher.Model.MalProxy.Consolidators
{
    /*internal interface IAnimeListConsolidator
    {
        void Consolidate(HashSet<MyAnimeEntry> animeList, AnimeDbContext context);
        void Consolidate(MyAnimeEntry databaseEntry, MyAnimeEntry pulledEntry);
    }*/
}
